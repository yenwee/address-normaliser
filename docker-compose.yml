services:
  app:
    build: .
    env_file: .env
    volumes:
      - ./credentials:/app/credentials
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
